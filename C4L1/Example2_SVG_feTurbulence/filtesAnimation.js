"use strict";

gsap.to("#feTurbulence", 20, {attr:{baseFrequency:0.04}, repeat: -1})

// let frequency = 0.02;
// let element = document.getElementById("feTurbulence");

// function animate() {
//   let timing = setInterval(currentAnimationFrame, 1);
//   function currentAnimationFrame() {
//     if (frequency > 0.08) {
//       clearInterval(timing);
//     } else {
//       frequency+=0.001;
//       element.setAttribute("baseFrequency", frequency);
//     }
//   }
// };

// animate();























